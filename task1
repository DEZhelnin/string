#include<iostream>
#include<string>
using namespace std;
int main(){
    setlocale(LC_ALL, "Rus");
    string c,  a;
    int i;
    cin >> c;
    for (i = c.length() - 1; i >= 0; i--)
    {
        a = a + c[i];
    }
    if (c == a)cout << "да";
    else cout << "нет";
}
